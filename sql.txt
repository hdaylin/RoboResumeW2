USE RESUME;  

drop table if exists PERSON;  
drop table if exists EDUCATION;  
drop table if exists SKILLS;  
drop table if exists EXPERIENCE ;  

CREATE TABLE PERSON(
PERSON_ID INTEGER auto_increment, 
FNAME VARCHAR(250) NOT NULL, 
LNAME VARCHAR(250) NOT NULL, 
EMAIL VARCHAR(250) NOT NULL,
PRIMARY KEY(PERSON_ID)); 

INSERT INTO PERSON (FNAME, LNAME, EMAIL) VALUE
("Daylin", "Henry", "djhenry94@gmail.com");

CREATE TABLE EDUCATION(
EDU_ID INTEGER auto_increment, 
PERSON_ID INTEGER NOT NULL, 
SCHOOL VARCHAR(256),
DEGREE_NAME VARCHAR(999) NOT NULL, 
D_YEAR VARCHAR(8) NOT NULL, 
PRIMARY KEY(EDU_ID)); 

INSERT INTO EDUCATION (PERSON_ID, DEGREE_NAME, D_YEAR)VALUES 
(2, "MANAGEMENT", "2012");


CREATE TABLE SKILLS(
SKILL_ID INTEGER auto_increment, 
SKILL_NAME VARCHAR(655) NOT NULL, 
SLEVEL VARCHAR(689),
PERSON_ID INTEGER NOT NULL,
primary key (SKILL_ID)); 

INSERT INTO SKILLS VALUES 
(121, "PHP","Perfect", 1), (123, "PROJECT MANAGEMENT","Perfect", 1); 

CREATE TABLE EXPERIENCE(
EXP_ID  INTEGER auto_increment, 
EXP VARCHAR(999)  NOT NULL,  
DUTY1 VARCHAR(999), 
DUTY2 VARCHAR(999), 
PERSON_ID INTEGER NOT NULL,
PRIMARY KEY (EXP_ID));  

INSERT INTO EXPERIENCE(EXP, DUTY1, DUTY2, PERSON_ID) VALUES
("WELLS FARGO", "Banker", "MANAGER", 1), ("PWC","INTERN", "CONSULTANT", 1) ;


SELECT* FROM EDUCATION;